// 4chan launched on October 1, 2003.
// 4chan launched on October 1, 2003.
export function isChanBirthday(date: Date = new Date()): boolean {
  return date.getMonth() === 9 && date.getDate() === 1
}
